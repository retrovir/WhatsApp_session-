# StackHost configuration file

# Version: 2.0.0

user:
  name: "retrovir"
  github: "retrovir"

runtime:
  image: "node:22"

commands:
  package: "" 
  build:
    - "npm install"
  start: "npm start"

repository:
  branch: "main"
  auto_deploy: true

env:
  - PORT:3000
  
