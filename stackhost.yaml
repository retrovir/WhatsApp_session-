user:
  name: "ishikki"
  github: "retrovir"

runtime:
  image: "python:3.11-slim"

commands:
  package: "nodejs npm"
  build:
    - "npm install"
  start: "node index.js"

repository:
  branch: "main"
  auto_deploy: true
